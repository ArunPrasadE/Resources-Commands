---
tags:
  - command-list
  - cybersecurity
  - linux
---
```bash
# Update packages
sudo apt update -y && sudo apt upgrade -y
sudo apt autoremove -y

# Update Kali OS
sudo apt update && sudo dist-upgrade -y
```
